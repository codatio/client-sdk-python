"""Top-level composed client for Bank Feeds.

Generated by pipeline/postprocess_generated.py — do not edit by hand.
"""

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Callable, Dict, Mapping, Optional, Union

from codat_bankfeeds.api_client import ApiClient
from codat_bankfeeds.configuration import Configuration
from codat_bankfeeds._hooks import SDKHooks, attach_hooks_to_api_client
from codat_bankfeeds.types import UNSET, OptionalNullable
from codat_bankfeeds.utils.retries import RetryConfig

if TYPE_CHECKING:
    from codat_bankfeeds.httpclient import AsyncHttpClient, HttpClient

from codat_bankfeeds.api.companies import Companies
from codat_bankfeeds.api.connections import Connections
from codat_bankfeeds.api.bank_accounts import BankAccounts
from codat_bankfeeds.api.source_accounts import SourceAccounts
from codat_bankfeeds.api.account_mapping import AccountMapping
from codat_bankfeeds.api.transactions import Transactions
from codat_bankfeeds.api.company_information import CompanyInformation
from codat_bankfeeds.api.managed_bank_feeds import ManagedBankFeeds


_DEFAULT_TIMEOUT_MS = 5000


class CodatBankFeeds:
    r"""Bank Feeds: Bank Feeds solution enables your SMB users to set up bank feeds from accounts in your application to supported accounting software.

A bank feed is a connection between a source bank account in your application and a target bank account in a supported accounting software.

[Explore solution](https://docs.codat.io/bank-feeds-api/overview) | [See OpenAPI spec](https://github.com/codatio/oas)

---
<!-- Start Codat Tags Table -->
## Endpoints

| Endpoints | Description |
| :- |:- |
| Companies | Create and manage your SMB users' companies. |
| Connections | Create new and manage existing data connections for a company. |
| Source accounts | Provide and manage lists of source bank accounts. |
| Account mapping | Extra functionality for building an account management UI. |
| Company information | Get detailed information about a company from the underlying platform. |
| Transactions | Create new bank account transactions for a company's connections, and see previous operations. |
<!-- End Codat Tags Table -->"""

    companies: "Companies"
    r"""Create and manage your SMB users' companies."""
    connections: "Connections"
    r"""Create new and manage existing data connections for a company."""
    bank_accounts: "BankAccounts"
    r"""Access bank accounts in an SMBs accounting software."""
    source_accounts: "SourceAccounts"
    r"""Provide and manage lists of source bank accounts."""
    account_mapping: "AccountMapping"
    r"""Extra functionality for building an account management UI."""
    transactions: "Transactions"
    r"""Create new bank account transactions for a company's connections, and see previous operations."""
    company_information: "CompanyInformation"
    r"""Get detailed information about a company from the underlying accounting software."""
    managed_bank_feeds: "ManagedBankFeeds"
    r"""Manage bank feed syncs for source accounts."""

    def __init__(
        self,
        security: Optional[Any] = None,
        server_idx: Optional[int] = None,
        url_params: Optional[Dict[str, str]] = None,
        server_url: Optional[str] = None,
        client: Optional[HttpClient] = None,
        async_client: Optional[AsyncHttpClient] = None,
        retry_config: OptionalNullable[RetryConfig] = UNSET,
        timeout_ms: Optional[int] = None,
        debug_logger: Optional[Any] = None,
    ) -> None:
        self._security = security
        self._retry_config = retry_config
        self._timeout_ms = timeout_ms
        self._debug_logger = debug_logger
        if async_client is not None:
            import warnings as _w
            _w.warn(
                "the async_client kwarg is accepted for Speakeasy-SDK compatibility "
                "but not used: the bare async methods (e.g. .companies.list_async(...)) "
                "run the sync stack in a worker thread, where an httpx.AsyncClient "
                "cannot be driven safely. Pass client= (an httpx.Client) instead — it "
                "carries every request, including the *_async methods.",
                stacklevel=2,
            )

        config = Configuration(host=server_url) if server_url else Configuration()
        self._api_client = ApiClient(configuration=config)
        if client is not None:
            from codat_bankfeeds._httpx_transport import HttpxRestClient
            self._api_client.rest_client = HttpxRestClient(client)
        try:
            from codat_bankfeeds._version import __user_agent__ as _ua
            self._api_client.set_default_header("User-Agent", _ua)
        except Exception:
            pass
        self._hooks = SDKHooks()
        attach_hooks_to_api_client(
            self._api_client,
            self._hooks,
            base_url=(server_url or ""),
            security_source=security,
            auth_headers=[('auth_header', 'Authorization')],
        )

        self.companies = Companies(api_client=self._api_client)
        self.connections = Connections(api_client=self._api_client)
        self.bank_accounts = BankAccounts(api_client=self._api_client)
        self.source_accounts = SourceAccounts(api_client=self._api_client)
        self.account_mapping = AccountMapping(api_client=self._api_client)
        self.transactions = Transactions(api_client=self._api_client)
        self.company_information = CompanyInformation(api_client=self._api_client)
        self.managed_bank_feeds = ManagedBankFeeds(api_client=self._api_client)

        # Attach the SDK-level retry + timeout defaults to each method-group so a
        # per-call retries=UNSET / timeout_ms=None falls back to them. When no
        # timeout_ms is set the default is _DEFAULT_TIMEOUT_MS (matches the shipped SDK).
        _sdk_retry_default = retry_config if not isinstance(retry_config, type(UNSET)) else None
        _sdk_timeout_default = timeout_ms if timeout_ms is not None else _DEFAULT_TIMEOUT_MS
        _group_api_prefix = type(self).__module__.rsplit(".", 1)[0] + ".api."
        _defaults_seen: set = set()

        def _attach_sdk_defaults(_grp) -> None:
            if id(_grp) in _defaults_seen:
                return
            _defaults_seen.add(id(_grp))
            object.__setattr__(_grp, "_sdk_retry_config", _sdk_retry_default)
            object.__setattr__(_grp, "_sdk_timeout_ms", _sdk_timeout_default)
            for _name, _child in list(vars(_grp).items()):
                if not _name.startswith("_") and type(_child).__module__.startswith(_group_api_prefix):
                    _attach_sdk_defaults(_child)

        for _grp in (
            self.companies,
            self.connections,
            self.bank_accounts,
            self.source_accounts,
            self.account_mapping,
            self.transactions,
            self.company_information,
            self.managed_bank_feeds,
        ):
            _attach_sdk_defaults(_grp)

    def __enter__(self) -> "CodatBankFeeds":
        self._api_client.__enter__()
        return self

    def __exit__(self, exc_type, exc, tb) -> None:
        self._api_client.__exit__(exc_type, exc, tb)

    async def __aenter__(self) -> "CodatBankFeeds":
        return self

    async def __aexit__(self, exc_type, exc, tb) -> None:
        self._api_client.__exit__(exc_type, exc, tb)
