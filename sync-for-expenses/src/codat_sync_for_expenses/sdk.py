"""Top-level composed client for Expenses.

Generated by pipeline/postprocess_generated.py — do not edit by hand.
"""

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Callable, Dict, Mapping, Optional, Union

from codat_sync_for_expenses.api_client import ApiClient
from codat_sync_for_expenses.configuration import Configuration
from codat_sync_for_expenses._hooks import SDKHooks, attach_hooks_to_api_client
from codat_sync_for_expenses.types import UNSET, OptionalNullable
from codat_sync_for_expenses.utils.retries import RetryConfig

if TYPE_CHECKING:
    from codat_sync_for_expenses.httpclient import AsyncHttpClient, HttpClient

from codat_sync_for_expenses.api.companies import Companies
from codat_sync_for_expenses.api.connections import Connections
from codat_sync_for_expenses.api.company_info import CompanyInfo
from codat_sync_for_expenses.api.manage_data import ManageData
from codat_sync_for_expenses.api.mapping_options import MappingOptions
from codat_sync_for_expenses.api.accounts import Accounts
from codat_sync_for_expenses.api.bank_accounts import BankAccounts
from codat_sync_for_expenses.api.customers import Customers
from codat_sync_for_expenses.api.suppliers import Suppliers
from codat_sync_for_expenses.api.push_operations import PushOperations
from codat_sync_for_expenses.api.configuration import Configuration as ConfigurationApi
from codat_sync_for_expenses.api.expenses import Expenses
from codat_sync_for_expenses.api.reimbursements import Reimbursements
from codat_sync_for_expenses.api.transfers import Transfers
from codat_sync_for_expenses.api.adjustments import Adjustments
from codat_sync_for_expenses.api.attachments import Attachments
from codat_sync_for_expenses.api.sync import Sync
from codat_sync_for_expenses.api.transaction_status import TransactionStatus


_DEFAULT_TIMEOUT_MS = 5000


class CodatSyncExpenses:
    r"""Expenses: The API for Codat's Expenses solution.

Expenses is an API and a set of supporting tools. It has been built to
enable corporate card and expense management platforms to provide high-quality
integrations with multiple accounting software through a standardized API.

[Explore solution](https://docs.codat.io/sync-for-expenses/overview) | [See our OpenAPI spec](https://github.com/codatio/oas)

Not seeing the endpoints you're expecting? We've [reorganized our solutions](https://docs.codat.io/updates/230901-new-products), and you may be using a [different version of Expenses](https://docs.codat.io/sync-for-expenses-v1-api#/).

---
<!-- Start Codat Tags Table -->
## Endpoints

| Endpoints | Description |
| :- |:- |
| Companies | Create and manage your SMB users' companies. |
| Connections | Create new and manage existing data connections for a company. |
| Configuration | View and manage mapping configuration and defaults for expense transactions. |
| Sync | Monitor the status of data syncs. |
| Expenses | Create and update transactions that represent your customers' spend. |
| Transfers | Create and update transactions that represent the movement of your customers' money. |
| Reimbursements | Create and update transactions that represent your customers' repayable spend. |
| Attachments | Attach receipts to a transaction for a complete audit trail. |
| Transaction status | Monitor the status of individual transactions in data syncs. |
| Manage data | Control and monitor the retrieval of data from an integration. |
| Push operations | View historic push operations. |
| Accounts | Create accounts and view account schemas. |
| Customers | Get, create, and update customers. |
| Suppliers | Get, create, and update suppliers. |
<!-- End Codat Tags Table -->"""

    companies: "Companies"
    r"""Create and manage your SMB users' companies."""
    connections: "Connections"
    r"""Create new and manage existing data connections for a company."""
    company_info: "CompanyInfo"
    r"""View the company information of your customers' linked accounting software."""
    manage_data: "ManageData"
    r"""Control and monitor the retrieval of data from an integration."""
    mapping_options: "MappingOptions"
    r"""Get accounts, trackingCategories and taxRates that can be used in your create and update requests."""
    accounts: "Accounts"
    r"""Create accounts and view create account options."""
    bank_accounts: "BankAccounts"
    r"""Create bank accounts and view create bank account options."""
    customers: "Customers"
    r"""Get, create, and update customers."""
    suppliers: "Suppliers"
    r"""Get, create, and update suppliers."""
    push_operations: "PushOperations"
    r"""View historic push operations."""
    configuration: "ConfigurationApi"
    r"""View and manage mapping configuration and defaults for expense transactions."""
    expenses: "Expenses"
    r"""Create and update transactions that represent your customers' spend."""
    reimbursements: "Reimbursements"
    r"""Create and update transactions that represent your customers' repayable spend."""
    transfers: "Transfers"
    r"""Create and update transactions that represent the movement of your customers' money."""
    adjustments: "Adjustments"
    r"""Create transactions that represent your adjustments to your customers' spend."""
    attachments: "Attachments"
    r"""Attach receipts to a transaction for a complete audit trail."""
    sync: "Sync"
    r"""Monitor the status of data syncs."""
    transaction_status: "TransactionStatus"
    r"""Monitor the status of individual transactions in data syncs."""

    def __init__(
        self,
        security: Optional[Any] = None,
        server_idx: Optional[int] = None,
        url_params: Optional[Dict[str, str]] = None,
        server_url: Optional[str] = None,
        client: Optional[HttpClient] = None,
        async_client: Optional[AsyncHttpClient] = None,
        retry_config: OptionalNullable[RetryConfig] = UNSET,
        timeout_ms: Optional[int] = None,
        debug_logger: Optional[Any] = None,
    ) -> None:
        self._security = security
        self._retry_config = retry_config
        self._timeout_ms = timeout_ms
        self._debug_logger = debug_logger
        if async_client is not None:
            import warnings as _w
            _w.warn(
                "the async_client kwarg is accepted for Speakeasy-SDK compatibility "
                "but not used: the bare async methods (e.g. .companies.list_async(...)) "
                "run the sync stack in a worker thread, where an httpx.AsyncClient "
                "cannot be driven safely. Pass client= (an httpx.Client) instead — it "
                "carries every request, including the *_async methods.",
                stacklevel=2,
            )

        config = Configuration(host=server_url) if server_url else Configuration()
        self._api_client = ApiClient(configuration=config)
        if client is not None:
            from codat_sync_for_expenses._httpx_transport import HttpxRestClient
            self._api_client.rest_client = HttpxRestClient(client)
        try:
            from codat_sync_for_expenses._version import __user_agent__ as _ua
            self._api_client.set_default_header("User-Agent", _ua)
        except Exception:
            pass
        self._hooks = SDKHooks()
        attach_hooks_to_api_client(
            self._api_client,
            self._hooks,
            base_url=(server_url or ""),
            security_source=security,
            auth_headers=[('auth_header', 'Authorization')],
        )

        self.companies = Companies(api_client=self._api_client)
        self.connections = Connections(api_client=self._api_client)
        self.company_info = CompanyInfo(api_client=self._api_client)
        self.manage_data = ManageData(api_client=self._api_client)
        self.mapping_options = MappingOptions(api_client=self._api_client)
        self.accounts = Accounts(api_client=self._api_client)
        self.bank_accounts = BankAccounts(api_client=self._api_client)
        self.customers = Customers(api_client=self._api_client)
        self.suppliers = Suppliers(api_client=self._api_client)
        self.push_operations = PushOperations(api_client=self._api_client)
        self.configuration = ConfigurationApi(api_client=self._api_client)
        self.expenses = Expenses(api_client=self._api_client)
        self.reimbursements = Reimbursements(api_client=self._api_client)
        self.transfers = Transfers(api_client=self._api_client)
        self.adjustments = Adjustments(api_client=self._api_client)
        self.attachments = Attachments(api_client=self._api_client)
        self.sync = Sync(api_client=self._api_client)
        self.transaction_status = TransactionStatus(api_client=self._api_client)

        # Attach the SDK-level retry + timeout defaults to each method-group so a
        # per-call retries=UNSET / timeout_ms=None falls back to them. When no
        # timeout_ms is set the default is _DEFAULT_TIMEOUT_MS (matches the shipped SDK).
        _sdk_retry_default = retry_config if not isinstance(retry_config, type(UNSET)) else None
        _sdk_timeout_default = timeout_ms if timeout_ms is not None else _DEFAULT_TIMEOUT_MS
        _group_api_prefix = type(self).__module__.rsplit(".", 1)[0] + ".api."
        _defaults_seen: set = set()

        def _attach_sdk_defaults(_grp) -> None:
            if id(_grp) in _defaults_seen:
                return
            _defaults_seen.add(id(_grp))
            object.__setattr__(_grp, "_sdk_retry_config", _sdk_retry_default)
            object.__setattr__(_grp, "_sdk_timeout_ms", _sdk_timeout_default)
            for _name, _child in list(vars(_grp).items()):
                if not _name.startswith("_") and type(_child).__module__.startswith(_group_api_prefix):
                    _attach_sdk_defaults(_child)

        for _grp in (
            self.companies,
            self.connections,
            self.company_info,
            self.manage_data,
            self.mapping_options,
            self.accounts,
            self.bank_accounts,
            self.customers,
            self.suppliers,
            self.push_operations,
            self.configuration,
            self.expenses,
            self.reimbursements,
            self.transfers,
            self.adjustments,
            self.attachments,
            self.sync,
            self.transaction_status,
        ):
            _attach_sdk_defaults(_grp)

    def __enter__(self) -> "CodatSyncExpenses":
        self._api_client.__enter__()
        return self

    def __exit__(self, exc_type, exc, tb) -> None:
        self._api_client.__exit__(exc_type, exc, tb)

    async def __aenter__(self) -> "CodatSyncExpenses":
        return self

    async def __aexit__(self, exc_type, exc, tb) -> None:
        self._api_client.__exit__(exc_type, exc, tb)
