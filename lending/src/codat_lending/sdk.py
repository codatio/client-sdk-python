"""Top-level composed client for Lending.

Generated by pipeline/postprocess_generated.py — do not edit by hand.
"""

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Callable, Dict, Mapping, Optional, Union

from codat_lending.api_client import ApiClient
from codat_lending.configuration import Configuration
from codat_lending._hooks import SDKHooks, attach_hooks_to_api_client
from codat_lending.types import UNSET, OptionalNullable
from codat_lending.utils.retries import RetryConfig

if TYPE_CHECKING:
    from codat_lending.httpclient import AsyncHttpClient, HttpClient

from codat_lending.api.accounting_bank_data import AccountingBankData
from codat_lending.api.accounts_payable import AccountsPayable
from codat_lending.api.accounts_receivable import AccountsReceivable
from codat_lending.api.banking import Banking
from codat_lending.api.financial_statements import FinancialStatements
from codat_lending.api.loan_writeback import LoanWriteback
from codat_lending.api.manage_data import ManageData
from codat_lending.api.sales import Sales
from codat_lending.api.transactions_group import TransactionsGroup
from codat_lending.api.companies import Companies
from codat_lending.api.connections import Connections
from codat_lending.api.bank_statements import BankStatements
from codat_lending.api.liabilities import Liabilities
from codat_lending.api.company_info import CompanyInfo
from codat_lending.api.data_integrity import DataIntegrity
from codat_lending.api.excel_reports import ExcelReports
from codat_lending.api.file_upload import FileUpload
from codat_lending.api.manage_reports import ManageReports
from codat_lending.api.financial_summary import FinancialSummary


_DEFAULT_TIMEOUT_MS = 5000


class CodatLending:
    r"""Lending: Our Lending solution helps you make smarter credit decisions on small businesses by enabling you to pull your customers' latest data from accounting, banking, and commerce software they are already using. It also includes features to help providers verify the accuracy of data and process it more efficiently.

The Lending solution is built on top of the latest accounting, commerce, and banking data, providing you with the most important data points you need to get a full picture of SMB creditworthiness and make a comprehensive assessment of your customers.

[Explore solution](https://docs.codat.io/lending/overview) | [See OpenAPI spec](https://github.com/codatio/oas)

<!-- Start Codat Tags Table -->
## Endpoints

| Endpoints | Description |
| :- |:- |
| Companies | Create and manage your SMB users' companies. |
| Connections | Create new and manage existing data connections for a company. |
| Bank statements | Retrieve banking data from linked bank accounts. |
| Sales | Retrieve standardized sales data from a linked commerce software. |
| Financial statements | Financial data and reports from a linked accounting software. |
| Liabilities | Debt and other liabilities. |
| Accounts payable | Data from a linked accounting software representing money the business owes money to its suppliers. |
| Accounts receivable | Data from a linked accounting software representing money owed to the business for sold goods or services. |
| Transactions | Data from a linked accounting software representing transactions. |
| Company info | View company information fetched from the source platform. |
| Data integrity | Match mutable accounting data with immutable banking data to increase confidence in financial data. |
| Excel reports | Download reports in Excel format. |
| Manage data | Control how data is retrieved from an integration. |
| File upload | Endpoints to manage uploaded files. |
| Loan writeback | Implement the [loan writeback](https://docs.codat.io/lending/guides/loan-writeback/introduction) procedure in your lending process to maintain an accurate position of a loan during the entire lending cycle. |
<!-- End Codat Tags Table -->"""

    accounting_bank_data: "AccountingBankData"
    r""""""
    accounts_payable: "AccountsPayable"
    r""""""
    accounts_receivable: "AccountsReceivable"
    r""""""
    banking: "Banking"
    r""""""
    financial_statements: "FinancialStatements"
    r""""""
    loan_writeback: "LoanWriteback"
    r""""""
    manage_data: "ManageData"
    r""""""
    sales: "Sales"
    r""""""
    transactions: "TransactionsGroup"
    r""""""
    companies: "Companies"
    r"""Create and manage your SMB users' companies."""
    connections: "Connections"
    r"""Create new and manage existing data connections for a company."""
    bank_statements: "BankStatements"
    r"""Retrieve banking data from linked bank accounts."""
    liabilities: "Liabilities"
    r"""Debt and other liabilities."""
    company_info: "CompanyInfo"
    r"""View company information fetched from the source platform."""
    data_integrity: "DataIntegrity"
    r"""Match mutable accounting data with immutable banking data to increase confidence in financial data."""
    excel_reports: "ExcelReports"
    r"""Download reports in Excel format."""
    file_upload: "FileUpload"
    r"""Endpoints to manage uploaded files."""
    manage_reports: "ManageReports"
    r"""Generate and review generated reports for a company."""
    financial_summary: "FinancialSummary"
    r"""View financial summary information for a company, including credit model reports and accounting score."""

    def __init__(
        self,
        security: Optional[Any] = None,
        server_idx: Optional[int] = None,
        url_params: Optional[Dict[str, str]] = None,
        server_url: Optional[str] = None,
        client: Optional[HttpClient] = None,
        async_client: Optional[AsyncHttpClient] = None,
        retry_config: OptionalNullable[RetryConfig] = UNSET,
        timeout_ms: Optional[int] = None,
        debug_logger: Optional[Any] = None,
    ) -> None:
        self._security = security
        self._retry_config = retry_config
        self._timeout_ms = timeout_ms
        self._debug_logger = debug_logger
        if async_client is not None:
            import warnings as _w
            _w.warn(
                "the async_client kwarg is accepted for Speakeasy-SDK compatibility "
                "but not used: the bare async methods (e.g. .companies.list_async(...)) "
                "run the sync stack in a worker thread, where an httpx.AsyncClient "
                "cannot be driven safely. Pass client= (an httpx.Client) instead — it "
                "carries every request, including the *_async methods.",
                stacklevel=2,
            )

        config = Configuration(host=server_url) if server_url else Configuration()
        self._api_client = ApiClient(configuration=config)
        if client is not None:
            from codat_lending._httpx_transport import HttpxRestClient
            self._api_client.rest_client = HttpxRestClient(client)
        try:
            from codat_lending._version import __user_agent__ as _ua
            self._api_client.set_default_header("User-Agent", _ua)
        except Exception:
            pass
        self._hooks = SDKHooks()
        attach_hooks_to_api_client(
            self._api_client,
            self._hooks,
            base_url=(server_url or ""),
            security_source=security,
            auth_headers=[('auth_header', 'Authorization')],
        )

        self.accounting_bank_data = AccountingBankData(api_client=self._api_client)
        self.accounts_payable = AccountsPayable(api_client=self._api_client)
        self.accounts_receivable = AccountsReceivable(api_client=self._api_client)
        self.banking = Banking(api_client=self._api_client)
        self.financial_statements = FinancialStatements(api_client=self._api_client)
        self.loan_writeback = LoanWriteback(api_client=self._api_client)
        self.manage_data = ManageData(api_client=self._api_client)
        self.sales = Sales(api_client=self._api_client)
        self.transactions = TransactionsGroup(api_client=self._api_client)
        self.companies = Companies(api_client=self._api_client)
        self.connections = Connections(api_client=self._api_client)
        self.bank_statements = BankStatements(api_client=self._api_client)
        self.liabilities = Liabilities(api_client=self._api_client)
        self.company_info = CompanyInfo(api_client=self._api_client)
        self.data_integrity = DataIntegrity(api_client=self._api_client)
        self.excel_reports = ExcelReports(api_client=self._api_client)
        self.file_upload = FileUpload(api_client=self._api_client)
        self.manage_reports = ManageReports(api_client=self._api_client)
        self.financial_summary = FinancialSummary(api_client=self._api_client)

        # Attach the SDK-level retry + timeout defaults to each method-group so a
        # per-call retries=UNSET / timeout_ms=None falls back to them. When no
        # timeout_ms is set the default is _DEFAULT_TIMEOUT_MS (matches the shipped SDK).
        _sdk_retry_default = retry_config if not isinstance(retry_config, type(UNSET)) else None
        _sdk_timeout_default = timeout_ms if timeout_ms is not None else _DEFAULT_TIMEOUT_MS
        _group_api_prefix = type(self).__module__.rsplit(".", 1)[0] + ".api."
        _defaults_seen: set = set()

        def _attach_sdk_defaults(_grp) -> None:
            if id(_grp) in _defaults_seen:
                return
            _defaults_seen.add(id(_grp))
            object.__setattr__(_grp, "_sdk_retry_config", _sdk_retry_default)
            object.__setattr__(_grp, "_sdk_timeout_ms", _sdk_timeout_default)
            for _name, _child in list(vars(_grp).items()):
                if not _name.startswith("_") and type(_child).__module__.startswith(_group_api_prefix):
                    _attach_sdk_defaults(_child)

        for _grp in (
            self.accounting_bank_data,
            self.accounts_payable,
            self.accounts_receivable,
            self.banking,
            self.financial_statements,
            self.loan_writeback,
            self.manage_data,
            self.sales,
            self.transactions,
            self.companies,
            self.connections,
            self.bank_statements,
            self.liabilities,
            self.company_info,
            self.data_integrity,
            self.excel_reports,
            self.file_upload,
            self.manage_reports,
            self.financial_summary,
        ):
            _attach_sdk_defaults(_grp)

    def __enter__(self) -> "CodatLending":
        self._api_client.__enter__()
        return self

    def __exit__(self, exc_type, exc, tb) -> None:
        self._api_client.__exit__(exc_type, exc, tb)

    async def __aenter__(self) -> "CodatLending":
        return self

    async def __aexit__(self, exc_type, exc, tb) -> None:
        self._api_client.__exit__(exc_type, exc, tb)
