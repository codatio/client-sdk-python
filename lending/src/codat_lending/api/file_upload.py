# coding: utf-8

"""
    Lending

    Our Lending solution helps you make smarter credit decisions on small businesses by enabling you to pull your customers' latest data from accounting, banking, and commerce software they are already using. It also includes features to help providers verify the accuracy of data and process it more efficiently.  The Lending solution is built on top of the latest accounting, commerce, and banking data, providing you with the most important data points you need to get a full picture of SMB creditworthiness and make a comprehensive assessment of your customers.  [Explore solution](https://docs.codat.io/lending/overview) | [See OpenAPI spec](https://github.com/codatio/oas)  <!-- Start Codat Tags Table --> ## Endpoints  | Endpoints | Description | | :- |:- | | Companies | Create and manage your SMB users' companies. | | Connections | Create new and manage existing data connections for a company. | | Bank statements | Retrieve banking data from linked bank accounts. | | Sales | Retrieve standardized sales data from a linked commerce software. | | Financial statements | Financial data and reports from a linked accounting software. | | Liabilities | Debt and other liabilities. | | Accounts payable | Data from a linked accounting software representing money the business owes money to its suppliers. | | Accounts receivable | Data from a linked accounting software representing money owed to the business for sold goods or services. | | Transactions | Data from a linked accounting software representing transactions. | | Company info | View company information fetched from the source platform. | | Data integrity | Match mutable accounting data with immutable banking data to increase confidence in financial data. | | Excel reports | Download reports in Excel format. | | Manage data | Control how data is retrieved from an integration. | | File upload | Endpoints to manage uploaded files. | | Loan writeback | Implement the [loan writeback](https://docs.codat.io/lending/guides/loan-writeback/introduction) procedure in your lending process to maintain an accurate position of a loan during the entire lending cycle. | <!-- End Codat Tags Table -->

    The version of the OpenAPI document: 3.0.0
    Contact: support@codat.io
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501

import asyncio
import httpx
import warnings
from pydantic import BaseModel, model_serializer, validate_call, Field
from typing import Any, Dict, List, Mapping, Optional, Tuple, Union
from typing_extensions import Annotated, NotRequired, TypedDict

from pydantic import Field
from typing import List, Optional, Tuple, Union
from typing_extensions import Annotated
from codat_lending.models.shared.accounts import Accounts
from codat_lending.models.shared.companies import Companies
from codat_lending.models.shared.company import Company
from codat_lending.models.shared.connections import Connections
from codat_lending.models.shared.contact import Contact
from codat_lending.models.shared.file import File

from codat_lending.api_client import ApiClient, RequestSerialized
from codat_lending.api_response import ApiResponse
from codat_lending.exceptions import ApiException
from codat_lending.models.errors import ErrorMessage, SDKError
from codat_lending.models.errors.errormessage import ErrorMessageData as _ErrorMessageDataSchema
from codat_lending.rest import RESTResponseType
from codat_lending.types import UNSET, OptionalNullable
from codat_lending.utils.retries import RetryConfig, execute_with_retries




def _wrap_binary_to_httpx_response(data, content_type=None):
    """Wrap raw binary response data into an httpx.Response so callers
    can rely on .status_code / .headers / .content / .read() as they
    would with the Speakeasy SDK. The data we get from OAG is the
    decoded body bytes — status code is 200 by construction (errors
    raised earlier as SDKError). content_type is the op's 2xx media
    type, set as the response Content-Type header so a consumer reading
    it gets the media type Speakeasy carries.
    """
    if isinstance(data, httpx.Response):
        return data
    if data is None:
        content = b''
    elif isinstance(data, (bytes, bytearray, memoryview)):
        content = bytes(data)
    else:
        content = bytes(str(data), 'utf-8')
    headers = {'content-type': content_type} if content_type else None
    return httpx.Response(status_code=200, content=content, headers=headers)


from codat_lending.models.shared.fileupload import FileUpload as FileUploadModel
class DownloadFilesRequestTypedDict(TypedDict):
    """TypedDict variant of DownloadFilesRequest, for dict-literal request inputs."""
    company_id: Annotated[str, Field(description="Unique identifier for a company.")]
    date_: NotRequired[Annotated[Optional[str], Field(description="Only download files uploaded on this date.")]]


class DownloadFilesRequest(BaseModel):
    """Request envelope for download_files."""
    company_id: Annotated[str, Field(description="Unique identifier for a company.")]
    date_: Annotated[Optional[str], Field(description="Only download files uploaded on this date.")] = None

    @model_serializer(mode="wrap")
    def _serialize_drop_none(self, handler):
        serialized = handler(self)
        return {k: v for k, v in serialized.items() if v is not None}


class ListFilesRequestTypedDict(TypedDict):
    """TypedDict variant of ListFilesRequest, for dict-literal request inputs."""
    company_id: Annotated[str, Field(description="Unique identifier for a company.")]


class ListFilesRequest(BaseModel):
    """Request envelope for list_files."""
    company_id: Annotated[str, Field(description="Unique identifier for a company.")]

    @model_serializer(mode="wrap")
    def _serialize_drop_none(self, handler):
        serialized = handler(self)
        return {k: v for k, v in serialized.items() if v is not None}


class UploadFilesRequestTypedDict(TypedDict):
    """TypedDict variant of UploadFilesRequest, for dict-literal request inputs."""
    company_id: Annotated[str, Field(description="Unique identifier for a company.")]
    connection_id: Annotated[str, Field(description="Unique identifier for a connection.")]
    file_upload: NotRequired[Optional[FileUploadModel]]


class UploadFilesRequest(BaseModel):
    """Request envelope for upload_files."""
    company_id: Annotated[str, Field(description="Unique identifier for a company.")]
    connection_id: Annotated[str, Field(description="Unique identifier for a connection.")]
    file_upload: Optional[FileUploadModel] = None

    @model_serializer(mode="wrap")
    def _serialize_drop_none(self, handler):
        serialized = handler(self)
        return {k: v for k, v in serialized.items() if v is not None}


class FileUpload:
    """NOTE: This class is auto generated by OpenAPI Generator
    Ref: https://openapi-generator.tech

    Do not edit the class manually.
    """

    def __init__(self, api_client=None) -> None:
        if api_client is None:
            api_client = ApiClient.get_default()
        self.api_client = api_client


    @validate_call(config={"arbitrary_types_allowed": True})
    def download(
        self,
        *,
        request: Union['DownloadFilesRequest', 'DownloadFilesRequestTypedDict'],
        retries: OptionalNullable[RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> httpx.Response:
        """Download all files for a company""" # noqa: E501

        if isinstance(request, dict):
            request = DownloadFilesRequest.model_validate(request)

        _effective_timeout_ms = timeout_ms if timeout_ms is not None else getattr(self, "_sdk_timeout_ms", None)
        _timeout_s = (_effective_timeout_ms / 1000.0) if _effective_timeout_ms is not None else None
        _request_timeout = (_timeout_s, _timeout_s) if _timeout_s is not None else None
        _server_override_token = None
        if server_url is not None:
            _server_override_token = self.api_client.configuration.host
            self.api_client.configuration.host = server_url
        try:
            _param = self._download_files_serialize(
                company_id=request.company_id,
                date_=request.date_,
                _request_auth=None,
                _content_type=None,
                _headers=dict(http_headers) if http_headers else None,
                _host_index=0,
            )

            _response_types_map: Dict[str, Optional[str]] = {
                '200': "bytearray",
                '400': "ErrorMessage",
                '401': "ErrorMessage",
                '402': "ErrorMessage",
                '403': "ErrorMessage",
                '404': "ErrorMessage",
                '429': "ErrorMessage",
                '500': "ErrorMessage",
                '503': "ErrorMessage",
            }
        finally:
            if _server_override_token is not None:
                self.api_client.configuration.host = _server_override_token

        _effective_retries = retries if not isinstance(retries, type(UNSET)) else getattr(self, "_sdk_retry_config", None)
        if isinstance(_effective_retries, type(UNSET)):
            _effective_retries = None

        def _do_call():
            try:
                response_data = self.api_client.call_api(
                    *_param,
                    _request_timeout=_request_timeout,
                    operation_id='download-files',
                )
                response_data.read()
                _result = self.api_client.response_deserialize(
                    response_data=response_data,
                    response_types_map=_response_types_map,
                )
                # Speakeasy raises SDKError for any status the operation does not document,
                # including an unexpected 2xx; OAG's response_deserialize would instead return
                # None. Match Speakeasy: if neither the exact status nor its NXX wildcard is
                # documented, raise. A documented no-content status is a key mapping to None,
                # so it stays and returns None.
                _ok_status = getattr(response_data, "status", None)
                if (str(_ok_status) not in _response_types_map
                        and f"{str(_ok_status)[0]}XX" not in _response_types_map):
                    _ok_body = getattr(response_data, "data", b"") or b""
                    raise SDKError(
                        "Unexpected response received",
                        raw_response=response_data,
                        status_code=_ok_status,
                        body=_ok_body.decode("utf-8", "replace") if isinstance(_ok_body, (bytes, bytearray)) else str(_ok_body),
                        headers=dict(response_data.getheaders() or {}),
                    )
                return _result.data
            except ApiException as _e:
                _body = getattr(_e, "body", "") or ""
                _status = getattr(_e, "status", 0) or 0
                _headers = dict(getattr(_e, "headers", None) or {})
                # Raise the typed Codat ErrorMessage only for the statuses this operation
                # documents as ErrorMessage, matching Speakeasy (which raises SDKError for any
                # undocumented status). Consumers can then do
                # `except errors.ErrorMessage as e: e.data.correlation_id`.
                if _response_types_map.get(str(_status)) == "ErrorMessage" and _body:
                    try:
                        _err_data = _ErrorMessageDataSchema.model_validate_json(_body)
                    except Exception:
                        _err_data = None
                    if _err_data is not None:
                        raise ErrorMessage(
                            data=_err_data,
                            status_code=_status,
                            body=_body,
                            headers=_headers,
                            raw_response=_e,
                        ) from _e
                raise SDKError(
                    message=getattr(_e, "reason", None) or str(_e),
                    status_code=_status,
                    body=_body,
                    headers=_headers,
                    raw_response=_e,
                ) from _e

        return _wrap_binary_to_httpx_response(execute_with_retries(_do_call, _effective_retries), 'application/octet-stream')


    @validate_call(config={"arbitrary_types_allowed": True})
    async def download_async(
        self,
        *,
        request: Union['DownloadFilesRequest', 'DownloadFilesRequestTypedDict'],
        retries: OptionalNullable[RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> httpx.Response:
        return await asyncio.get_event_loop().run_in_executor(
            None,
            lambda: self.download(
                request=request,
                retries=retries,
                server_url=server_url,
                timeout_ms=timeout_ms,
                http_headers=http_headers,
            ),
        )


    def _download_files_serialize(
        self,
        company_id,
        date_,
        _request_auth,
        _content_type,
        _headers,
        _host_index,
    ) -> RequestSerialized:

        _host = None

        _collection_formats: Dict[str, str] = {
        }

        _path_params: Dict[str, str] = {}
        _query_params: List[Tuple[str, str]] = []
        _header_params: Dict[str, Optional[str]] = _headers or {}
        _form_params: List[Tuple[str, str]] = []
        _files: Dict[
            str, Union[str, bytes, List[str], List[bytes], List[Tuple[str, bytes]]]
        ] = {}
        _body_params: Optional[bytes] = None

        # process the path parameters
        if company_id is not None:
            _path_params['companyId'] = company_id
        # process the query parameters
        if date_ is not None:
            
            _query_params.append(('date', date_))
            
        # process the header parameters
        # process the form parameters
        # process the body parameter


        if 'Accept' not in _header_params:
            _header_params['Accept'] = 'application/octet-stream'


        _auth_settings: List[str] = [
            'auth_header'
        ]

        return self.api_client.param_serialize(
            method='GET',
            resource_path='/companies/{companyId}/files/download',
            path_params=_path_params,
            query_params=_query_params,
            header_params=_header_params,
            body=_body_params,
            post_params=_form_params,
            files=_files,
            auth_settings=_auth_settings,
            collection_formats=_collection_formats,
            _host=_host,
            _request_auth=_request_auth
        )




    @validate_call(config={"arbitrary_types_allowed": True})
    def list_uploaded(
        self,
        *,
        request: Union['ListFilesRequest', 'ListFilesRequestTypedDict'],
        retries: OptionalNullable[RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> List[File]:
        """List all files uploaded by a company""" # noqa: E501

        if isinstance(request, dict):
            request = ListFilesRequest.model_validate(request)

        _effective_timeout_ms = timeout_ms if timeout_ms is not None else getattr(self, "_sdk_timeout_ms", None)
        _timeout_s = (_effective_timeout_ms / 1000.0) if _effective_timeout_ms is not None else None
        _request_timeout = (_timeout_s, _timeout_s) if _timeout_s is not None else None
        _server_override_token = None
        if server_url is not None:
            _server_override_token = self.api_client.configuration.host
            self.api_client.configuration.host = server_url
        try:
            _param = self._list_files_serialize(
                company_id=request.company_id,
                _request_auth=None,
                _content_type=None,
                _headers=dict(http_headers) if http_headers else None,
                _host_index=0,
            )

            _response_types_map: Dict[str, Optional[str]] = {
                '200': "List[File]",
                '401': "ErrorMessage",
                '402': "ErrorMessage",
                '403': "ErrorMessage",
                '404': "ErrorMessage",
                '429': "ErrorMessage",
                '500': "ErrorMessage",
                '503': "ErrorMessage",
            }
        finally:
            if _server_override_token is not None:
                self.api_client.configuration.host = _server_override_token

        _effective_retries = retries if not isinstance(retries, type(UNSET)) else getattr(self, "_sdk_retry_config", None)
        if isinstance(_effective_retries, type(UNSET)):
            _effective_retries = None

        def _do_call():
            try:
                response_data = self.api_client.call_api(
                    *_param,
                    _request_timeout=_request_timeout,
                    operation_id='list-files',
                )
                response_data.read()
                _result = self.api_client.response_deserialize(
                    response_data=response_data,
                    response_types_map=_response_types_map,
                )
                # Speakeasy raises SDKError for any status the operation does not document,
                # including an unexpected 2xx; OAG's response_deserialize would instead return
                # None. Match Speakeasy: if neither the exact status nor its NXX wildcard is
                # documented, raise. A documented no-content status is a key mapping to None,
                # so it stays and returns None.
                _ok_status = getattr(response_data, "status", None)
                if (str(_ok_status) not in _response_types_map
                        and f"{str(_ok_status)[0]}XX" not in _response_types_map):
                    _ok_body = getattr(response_data, "data", b"") or b""
                    raise SDKError(
                        "Unexpected response received",
                        raw_response=response_data,
                        status_code=_ok_status,
                        body=_ok_body.decode("utf-8", "replace") if isinstance(_ok_body, (bytes, bytearray)) else str(_ok_body),
                        headers=dict(response_data.getheaders() or {}),
                    )
                return _result.data
            except ApiException as _e:
                _body = getattr(_e, "body", "") or ""
                _status = getattr(_e, "status", 0) or 0
                _headers = dict(getattr(_e, "headers", None) or {})
                # Raise the typed Codat ErrorMessage only for the statuses this operation
                # documents as ErrorMessage, matching Speakeasy (which raises SDKError for any
                # undocumented status). Consumers can then do
                # `except errors.ErrorMessage as e: e.data.correlation_id`.
                if _response_types_map.get(str(_status)) == "ErrorMessage" and _body:
                    try:
                        _err_data = _ErrorMessageDataSchema.model_validate_json(_body)
                    except Exception:
                        _err_data = None
                    if _err_data is not None:
                        raise ErrorMessage(
                            data=_err_data,
                            status_code=_status,
                            body=_body,
                            headers=_headers,
                            raw_response=_e,
                        ) from _e
                raise SDKError(
                    message=getattr(_e, "reason", None) or str(_e),
                    status_code=_status,
                    body=_body,
                    headers=_headers,
                    raw_response=_e,
                ) from _e

        return execute_with_retries(_do_call, _effective_retries)


    @validate_call(config={"arbitrary_types_allowed": True})
    async def list_uploaded_async(
        self,
        *,
        request: Union['ListFilesRequest', 'ListFilesRequestTypedDict'],
        retries: OptionalNullable[RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> List[File]:
        return await asyncio.get_event_loop().run_in_executor(
            None,
            lambda: self.list_uploaded(
                request=request,
                retries=retries,
                server_url=server_url,
                timeout_ms=timeout_ms,
                http_headers=http_headers,
            ),
        )


    def _list_files_serialize(
        self,
        company_id,
        _request_auth,
        _content_type,
        _headers,
        _host_index,
    ) -> RequestSerialized:

        _host = None

        _collection_formats: Dict[str, str] = {
        }

        _path_params: Dict[str, str] = {}
        _query_params: List[Tuple[str, str]] = []
        _header_params: Dict[str, Optional[str]] = _headers or {}
        _form_params: List[Tuple[str, str]] = []
        _files: Dict[
            str, Union[str, bytes, List[str], List[bytes], List[Tuple[str, bytes]]]
        ] = {}
        _body_params: Optional[bytes] = None

        # process the path parameters
        if company_id is not None:
            _path_params['companyId'] = company_id
        # process the query parameters
        # process the header parameters
        # process the form parameters
        # process the body parameter


        if 'Accept' not in _header_params:
            _header_params['Accept'] = self.api_client.select_header_accept(
                [
                    'application/json'
                ]
            )


        _auth_settings: List[str] = [
            'auth_header'
        ]

        return self.api_client.param_serialize(
            method='GET',
            resource_path='/companies/{companyId}/files',
            path_params=_path_params,
            query_params=_query_params,
            header_params=_header_params,
            body=_body_params,
            post_params=_form_params,
            files=_files,
            auth_settings=_auth_settings,
            collection_formats=_collection_formats,
            _host=_host,
            _request_auth=_request_auth
        )




    @validate_call(config={"arbitrary_types_allowed": True})
    def upload(
        self,
        *,
        request: Union['UploadFilesRequest', 'UploadFilesRequestTypedDict'],
        retries: OptionalNullable[RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> None:
        """Upload files for a company""" # noqa: E501

        if isinstance(request, dict):
            request = UploadFilesRequest.model_validate(request)

        _effective_timeout_ms = timeout_ms if timeout_ms is not None else getattr(self, "_sdk_timeout_ms", None)
        _timeout_s = (_effective_timeout_ms / 1000.0) if _effective_timeout_ms is not None else None
        _request_timeout = (_timeout_s, _timeout_s) if _timeout_s is not None else None
        _server_override_token = None
        if server_url is not None:
            _server_override_token = self.api_client.configuration.host
            self.api_client.configuration.host = server_url
        try:
            _param = self._upload_files_serialize(
                company_id=request.company_id,
                connection_id=request.connection_id,
                file_upload=request.file_upload,
                _request_auth=None,
                _content_type=None,
                _headers=dict(http_headers) if http_headers else None,
                _host_index=0,
            )

            _response_types_map: Dict[str, Optional[str]] = {
                '200': None,
                '400': "ErrorMessage",
                '401': "ErrorMessage",
                '402': "ErrorMessage",
                '403': "ErrorMessage",
                '404': "ErrorMessage",
                '429': "ErrorMessage",
                '500': "ErrorMessage",
                '503': "ErrorMessage",
            }
        finally:
            if _server_override_token is not None:
                self.api_client.configuration.host = _server_override_token

        _effective_retries = retries if not isinstance(retries, type(UNSET)) else getattr(self, "_sdk_retry_config", None)
        if isinstance(_effective_retries, type(UNSET)):
            _effective_retries = None

        def _do_call():
            try:
                response_data = self.api_client.call_api(
                    *_param,
                    _request_timeout=_request_timeout,
                    operation_id='upload-files',
                )
                response_data.read()
                _result = self.api_client.response_deserialize(
                    response_data=response_data,
                    response_types_map=_response_types_map,
                )
                # Speakeasy raises SDKError for any status the operation does not document,
                # including an unexpected 2xx; OAG's response_deserialize would instead return
                # None. Match Speakeasy: if neither the exact status nor its NXX wildcard is
                # documented, raise. A documented no-content status is a key mapping to None,
                # so it stays and returns None.
                _ok_status = getattr(response_data, "status", None)
                if (str(_ok_status) not in _response_types_map
                        and f"{str(_ok_status)[0]}XX" not in _response_types_map):
                    _ok_body = getattr(response_data, "data", b"") or b""
                    raise SDKError(
                        "Unexpected response received",
                        raw_response=response_data,
                        status_code=_ok_status,
                        body=_ok_body.decode("utf-8", "replace") if isinstance(_ok_body, (bytes, bytearray)) else str(_ok_body),
                        headers=dict(response_data.getheaders() or {}),
                    )
                return _result.data
            except ApiException as _e:
                _body = getattr(_e, "body", "") or ""
                _status = getattr(_e, "status", 0) or 0
                _headers = dict(getattr(_e, "headers", None) or {})
                # Raise the typed Codat ErrorMessage only for the statuses this operation
                # documents as ErrorMessage, matching Speakeasy (which raises SDKError for any
                # undocumented status). Consumers can then do
                # `except errors.ErrorMessage as e: e.data.correlation_id`.
                if _response_types_map.get(str(_status)) == "ErrorMessage" and _body:
                    try:
                        _err_data = _ErrorMessageDataSchema.model_validate_json(_body)
                    except Exception:
                        _err_data = None
                    if _err_data is not None:
                        raise ErrorMessage(
                            data=_err_data,
                            status_code=_status,
                            body=_body,
                            headers=_headers,
                            raw_response=_e,
                        ) from _e
                raise SDKError(
                    message=getattr(_e, "reason", None) or str(_e),
                    status_code=_status,
                    body=_body,
                    headers=_headers,
                    raw_response=_e,
                ) from _e

        return execute_with_retries(_do_call, _effective_retries)


    @validate_call(config={"arbitrary_types_allowed": True})
    async def upload_async(
        self,
        *,
        request: Union['UploadFilesRequest', 'UploadFilesRequestTypedDict'],
        retries: OptionalNullable[RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> None:
        return await asyncio.get_event_loop().run_in_executor(
            None,
            lambda: self.upload(
                request=request,
                retries=retries,
                server_url=server_url,
                timeout_ms=timeout_ms,
                http_headers=http_headers,
            ),
        )


    def _upload_files_serialize(
        self,
        company_id,
        connection_id,
        file_upload,
        _request_auth,
        _content_type,
        _headers,
        _host_index,
    ) -> RequestSerialized:

        _host = None

        _collection_formats: Dict[str, str] = {
        }

        _path_params: Dict[str, str] = {}
        _query_params: List[Tuple[str, str]] = []
        _header_params: Dict[str, Optional[str]] = _headers or {}
        _form_params: List[Tuple[str, str]] = []
        _files: Dict[
            str, Union[str, bytes, List[str], List[bytes], List[Tuple[str, bytes]]]
        ] = {}
        _body_params: Optional[bytes] = None

        # process the path parameters
        if company_id is not None:
            _path_params['companyId'] = company_id
        if connection_id is not None:
            _path_params['connectionId'] = connection_id
        # process the query parameters
        # process the header parameters
        # process the form parameters
        if file_upload is not None and file_upload.file is not None:
            _cf = file_upload.file
            # 3-tuple carries content_type onto the multipart part (matching Speakeasy's
            # serialize_multipart_form); the 2-tuple lets files_parameters guess it.
            _files['file'] = (
                (_cf.file_name, _cf.content, _cf.content_type)
                if _cf.content_type is not None
                else (_cf.file_name, _cf.content)
            )
        # process the body parameter


        if 'Accept' not in _header_params:
            _header_params['Accept'] = self.api_client.select_header_accept(
                [
                    'application/json'
                ]
            )

        if _content_type:
            _header_params['Content-Type'] = _content_type
        else:
            _default_content_type = (
                self.api_client.select_header_content_type(
                    [
                        'multipart/form-data'
                    ]
                )
            )
            if _default_content_type is not None:
                _header_params['Content-Type'] = _default_content_type

        _auth_settings: List[str] = [
            'auth_header'
        ]

        return self.api_client.param_serialize(
            method='POST',
            resource_path='/companies/{companyId}/connections/{connectionId}/files',
            path_params=_path_params,
            query_params=_query_params,
            header_params=_header_params,
            body=_body_params,
            post_params=_form_params,
            files=_files,
            auth_settings=_auth_settings,
            collection_formats=_collection_formats,
            _host=_host,
            _request_auth=_request_auth
        )


